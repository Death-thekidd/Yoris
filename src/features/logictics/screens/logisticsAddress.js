import React from 'react';
import { LogisticContainer, LogisticPrimary } from '../components/address.styles';

export default function LogisticAddress() {
  return (
    <LogisticContainer>
      <LogisticPrimary>Save New Address?</LogisticPrimary>
    </LogisticContainer>
  );
};