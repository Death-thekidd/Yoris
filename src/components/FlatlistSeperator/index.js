import { View } from "react-native";

export default () => (
  <View
    style={{
      width: "100%",
      backgroundColor: "#C3AD60",
      borderBottomColor: "#C3AD60",
      borderBottomWidth: 0.5,
    }}
  />
);
